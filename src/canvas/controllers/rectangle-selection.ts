import BaseMovementController from './free-transform';

export default class RectangleSelectionController extends BaseMovementController {
    onEnter(): void {
        super.onEnter();
        
    }
}
